# Run a real local workflow through the authenticated Higgsfield CLI.
UGC_RUNTIME_MODE=local-cli

# Optional server-side OpenAI prompt improvement.
OPENAI_API_KEY=
OPENAI_PROMPT_MODEL=gpt-5.6-sol

# Vercel should use public-demo until a remote Higgsfield execution service is configured.
# UGC_RUNTIME_MODE=public-demo
# HF_CREDENTIALS=your_key_id:your_key_secret
